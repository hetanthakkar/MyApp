import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {},
  buttonText: {
    textAlign: "center",
    color: "blue",
    fontSize: 30,
  },
});

export default styles;
